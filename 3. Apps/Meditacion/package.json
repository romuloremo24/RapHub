{
  "name": "medita-app",
  "version": "1.0.0",
  "description": "Aplicación de meditación guiada",
  "private": true,
  "scripts": {
    "build": "echo 'No build step needed'",
    "sync": "npx cap sync android",
    "open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
